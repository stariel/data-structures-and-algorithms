'use strict';

function insertShiftArray(array, value) {
let arrLength = array.length;
let centerIndex = Math.round(arrLength);
  if ((typeof array !== 'object') || (typeof value == 'undefined')) {
    return null;
  }
}

module.exports = insertShiftArray;